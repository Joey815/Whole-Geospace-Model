# MAGE 与 WACCM-X 真实双向文件耦合闭环：PPT 12 行摘要

1. 截至 2026-03-26，`MAGE 1.25` 与 `WACCM-X(CESM)` 的**真实双向文件耦合闭环已完成验证**。
2. 本次闭环链路为：`real voltron.x -> CESM/WACCM-X import files -> real cesm.exe -> CESM feedback files/HDF5 -> second real voltron.x`。
3. 端到端桥接脚本 [run_bidirectional_cycle.sh](/home/jiaoy_group/jiaoy/data/MAGE1.25/experiments/cesm_kaiju_bridge/run_bidirectional_cycle.sh) 已在 [fixepot_e2e_20260325a](/home/jiaoy_group/jiaoy/data/MAGE1.25/experiments/cesm_kaiju_bridge/runs/fixepot_e2e_20260325a) 自然跑完整个闭环。
4. 同一入口随后又在 [replay_verify_20260325a](/home/jiaoy_group/jiaoy/data/MAGE1.25/experiments/cesm_kaiju_bridge/runs/replay_verify_20260325a) 成功复现，说明这不是一次性偶然结果，而是当前环境下可重复运行的真实链路。
5. 截至同一天，这条桥还在隔离主线 worktree [kaiju_waccmx_mainline](/home/jiaoy_group/jiaoy/data/MAGE1.25/experiments/kaiju_waccmx_mainline) 上完成了一次新的真实闭环：[mainline_geo_cycle_20260326a](/home/jiaoy_group/jiaoy/data/MAGE1.25/experiments/cesm_kaiju_bridge/runs/mainline_geo_cycle_20260326a)。
6. 随后，同样的 GEO 回传改动又被并回真正主仓库 [kaiju](/home/jiaoy_group/jiaoy/data/MAGE1.25/kaiju)，并用主仓库二进制 [build_gr/bin/voltron.x](/home/jiaoy_group/jiaoy/data/MAGE1.25/build_gr/bin/voltron.x) 完成了一次新的真实闭环：[mainrepo_geo_cycle_20260326a](/home/jiaoy_group/jiaoy/data/MAGE1.25/experiments/cesm_kaiju_bridge/runs/mainrepo_geo_cycle_20260326a)。
7. 这意味着成功不再只属于早期实验副本；主线风格 `voltron.x` 与真正主仓库 `voltron.x` 都已经能稳定完成正式 5 变量闭环，而 `neutral_rhs / NSRHS` 仍应视为与当前 live 正式主流程分开管理的实验增强通道；不过到 2026-03-30 为止，这条实验线里 `geo_sidecar` 的零场 blocker 也已通过 `post-d_pie_coupling refresh` 消除，当前剩下的问题已前移为量级/单位收敛，而不再是桥接断链。
8. 关键解锁点一：`MAGE epot` 已按 `WACCM-X` 所需全局磁网格正确写出，消除了此前的 `epot size mismatch`。
9. 关键解锁点二：`WACCM-X` external `epot` 分支先完成标准 `edynamo/ESMF` 初始化，再用 `MAGE epot` 覆盖，从而消除了此前的 `ESMF_FieldGet rc = 51`。
10. 闭环成立的直接证据是第二轮 `voltron` 电导已明显偏离默认值，例如 `mainrepo_geo_cycle_20260326a` 中 `SIGMAP` 由 `5.000..14.966 S` 变为 `0.138..17.678 S`，说明 `WACCM-X` 的 `SIGMAP/SIGMAH` 反馈已成功回写并改变 `MAGE/REMIX` 状态；而 `neutral_rhs / NSRHS` 目前只应按独立实验增强线表述，不计入当前 live 正式完成变量。
11. 到 2026-03-30，长期 continuation 的 blocker 已进一步缩小并定时：`Op-only` 会复现 `00300 -> 00600` 崩溃、`No-Op` 能成功到 `00600`，而且主案例与成功隔离 seed 在 `00000` 的 `Op/UI/VI/WI/TElec/TIon` 仍一致、到 `00300` 才明显分叉，因此当前决定性触发体已压缩到第一次 startup `0 -> 300 s` 内写坏的 `cam.r.00300 -> ShortLivedSpecie -> Op`；随后用 `178.793 -> 150.000` 的强 `epot` startup 单独复刻出来的新 `00300`，已经在状态上几乎贴住原始失败主案例。
12. 更关键的是，这个最小修复不只是一次性救活：在原始主案例失败谱系里只修 `ShortLivedSpecie -> Op`，已经连续成功跑过 `00300 -> 00600 -> 00900 -> 01200 -> 01500 -> 01800 -> 02100 -> 02400 -> 02700 -> 03000`；同时，上述 `178.793 -> 150.000` causal replay 生成的 `00300` 在隔离 continuation 中已再次复现“写出 `00600` restart 后 `rank 0 SIGSEGV`”这条原始失败签名。到 2026-03-30 的最新源码回溯又进一步表明，`chem_timestep_tend` 不是最后写坏 `Op` 的地方，当前最可疑的晚写路径已经收敛到 `cam_run2 -> ionosphere_run2 -> d_pie_coupling -> oplus_xport -> regrid_geo2phys_3d -> ShortLivedSpecies`。同日新做的“正式变量多轮闭环”也把这个分界线验证得更清楚：沿当前 live 主案例做真 `x1/x1/x1` 多轮时，seed 前向导出成功但 `cycle01 CESM` 立即复现老的 `00300 -> 00600 SIGSEGV`；而改用 `Op-repaired` 隔离谱系后，`03000 -> 03300 -> 03600 -> 03900` 三段 `CESM` continuation 全部成功，且对应 `cycle01_kaiju / cycle02_kaiju / cycle03_kaiju` 三次 `SIGMAP/SIGMAH` 回灌也全部成功。随后，一个可开关、可回退的 live `Op` repair hook 已接入真实桥脚本，并先在真实主流程 `x1/x1/x1, num_cycles=1` 上打通原来的 `cycle01` 崩点；紧接着又在同一 live 主流程上连续成功完成 `x1/x1/x1, num_cycles=3`，把 live 链路从 `00600` 进一步推进到 `00900 -> 01200 -> 01500`。而且这次三轮成功并不是每轮都在强制补丁：hook 日志显示它只是跳过了 `00600 / 00900 / 01200`，说明一旦用最小修复把 `00300 -> 00600` 跨过去，后续 live continuation 已经能够在正式变量闭环下连续稳定推进。
